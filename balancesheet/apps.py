from django.apps import AppConfig


class BalancesheetConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'balancesheet'
    verbose_name = 'Balance Sheet & Ledger'
